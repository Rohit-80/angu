import { NewdirecDirective } from './newdirec.directive';

describe('NewdirecDirective', () => {
  it('should create an instance', () => {
    const directive = new NewdirecDirective();
    expect(directive).toBeTruthy();
  });
});
